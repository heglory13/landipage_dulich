import { database } from "@/lib/database";
type User = { id:number; name:string; email:string; role:string; created_at:string };
export default function UsersPage(){const users=database.prepare("SELECT id,name,email,role,created_at FROM users ORDER BY created_at DESC").all() as User[];return <main className="mx-auto max-w-[1500px] px-4 py-7 md:px-7 lg:px-10 lg:py-10"><h1 className="text-3xl font-semibold">회원 관리</h1><p className="mt-2 text-[#646970]">등록된 관리자와 일반 회원을 확인합니다.</p><div className="mt-6 overflow-x-auto rounded-lg border border-[#c3c4c7] bg-white"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b bg-[#f6f7f7]"><tr><th className="p-4">이름</th><th>이메일</th><th>권한</th><th>가입일</th></tr></thead><tbody className="divide-y">{users.map(user=><tr key={user.id}><td className="p-4 font-semibold">{user.name}</td><td>{user.email}</td><td><span className={`rounded-full px-3 py-1 text-xs font-semibold ${user.role==='admin'?'bg-[#e7f3ff] text-[#135e96]':'bg-[#f0f0f1]'}`}>{user.role}</span></td><td>{new Date(user.created_at).toLocaleDateString("ko-KR")}</td></tr>)}</tbody></table></div></main>}
